<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Report Wizard Form -->
    <record id="view_dpr_report_wizard_form" model="ir.ui.view">
        <field name="name">dpr.report.wizard.form</field>
        <field name="model">dpr.report.wizard</field>
        <field name="arch" type="xml">
            <form>
                <group>
                    <group>
                        <field name="date_from"/>
                        <field name="date_to"/>
                        <field name="report_type" widget="selection"/>
                    </group>
                    <group>
                        <field name="project_ids" options="{'no_create': True, 'no_create_edit': True}" widget="many2many_tags"/>
                        <field name="output_format" widget="selection"/>
                    </group>
                </group>
                <group>
                    <field name="group_by"/>
                    <field name="include_photos"/>
                    <field name="include_weather"/>
                </group>
                <footer>
                    <button name="action_generate_report" string="Generate Report" type="object" class="oe_highlight"/>
                    <button string="Cancel" special="cancel"/>
                </footer>
            </form>
        </field>
    </record>

    <!-- Report Wizard Action -->
    <record id="action_dpr_report_wizard" model="ir.actions.act_window">
        <field name="name">DPR Report Wizard</field>
        <field name="res_model">dpr.report.wizard</field>
        <field name="view_mode">form</field>
    </record>


</odoo>
