<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <data>
        
        <!-- History Tree View -->
        <record id="purchase_requisition_history_tree_view" model="ir.ui.view">
            <field name="name">purchase.requisition.history.tree.view</field>
            <field name="model">purchase.requisition.history</field>
            <field name="arch" type="xml">
                <list decoration-info="to_state == 'draft'" 
                      decoration-warning="to_state == 'reject'"
                      decoration-success="to_state in ['po_confirm', 'approve']"
                      string="State History">
                    <field name="date"/>
                    <field name="state_label"/>
                    <field name="user_id"/>
                    <field name="from_state" optional="hide"/>
                    <field name="to_state" optional="hide"/>
                    <field name="notes"/>
                    <field name="ip_address" optional="hide"/>
                </list>
            </field>
        </record>

        <!-- History Form View -->
        <record id="purchase_requisition_history_form_view" model="ir.ui.view">
            <field name="name">purchase.requisition.history.form.view</field>
            <field name="model">purchase.requisition.history</field>
            <field name="arch" type="xml">
                <form>
                    <sheet>
                        <group>
                            <group>
                                <field name="requisition_id" readonly="1"/>
                                <field name="state_label" readonly="1"/>
                                <field name="user_id" readonly="1"/>
                                <field name="date" readonly="1"/>
                            </group>
                            <group>
                                <field name="from_state" readonly="1"/>
                                <field name="to_state" readonly="1"/>
                                <field name="ip_address" readonly="1"/>
                            </group>
                        </group>
                        <group>
                            <field name="notes" readonly="1" style="min-height: 100px;"/>
                        </group>
                    </sheet>
                </form>
            </field>
        </record>

        <!-- History Action -->
        <record model="ir.actions.act_window" id="action_purchase_requisition_history">
            <field name="name">Requisition State History</field>
            <field name="res_model">purchase.requisition.history</field>
            <field name="view_mode">list,form</field>
            <field name="domain">[('requisition_id', '=', context.get('active_id', False))]</field>
        </record>

    </data>
</odoo>